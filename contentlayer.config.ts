// Contentlayer configuration temporarily disabled due to build issues
// This will be re-enabled once Contentlayer dependencies are properly resolved

// For now, using static data structure in the blog pages
// TODO: Re-enable Contentlayer when dependencies are fixed

export const blogConfig = {
  contentDirPath: 'content/blog',
  enabled: false, // Set to true when Contentlayer is working
}
